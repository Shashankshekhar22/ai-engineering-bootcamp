# Skill Gap - Gemini Override

Small, additive adjustments layered on top of `../base.prompt.md` specifically for
Gemini models. Only include what actually differs - everything else is inherited from
the base prompt.

TODO: write Gemini-specific overrides, if any are needed.
