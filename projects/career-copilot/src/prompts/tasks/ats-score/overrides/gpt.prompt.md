# ATS Score - GPT Override

Small, additive adjustments layered on top of `../base.prompt.md` specifically for
GPT models. Only include what actually differs - everything else is inherited from
the base prompt.

TODO: write GPT-specific overrides, if any are needed.
