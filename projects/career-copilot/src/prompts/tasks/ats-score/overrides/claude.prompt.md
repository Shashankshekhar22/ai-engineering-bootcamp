# ATS Score - Claude Override

Small, additive adjustments layered on top of `../base.prompt.md` specifically for
Claude models. Only include what actually differs - everything else is inherited from
the base prompt.

TODO: write Claude-specific overrides, if any are needed.
