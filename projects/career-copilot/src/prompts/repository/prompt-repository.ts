// Prompt Repository: owns prompt storage, lookup, and versioning.
// Should NOT replace variables, compose fragments, or call OpenAI.
//
// Scaffold only - see notes/module-06/lesson-04.md for the full design.

export interface PromptRepository {
  // TODO: get(name: string, version?: string): string
}
